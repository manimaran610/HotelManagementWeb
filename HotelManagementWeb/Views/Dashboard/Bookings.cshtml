@model IEnumerable<HotelManagementWeb.Models.Booking>
@{
    ViewBag.Title = "Bookings";
}

<h2>Bookings</h2>
<a class="btn btn-warning pull-left" href="">Show Current Bookings only</a>
<a class="btn btn-danger pull-right">Remove completed Bookings</a>
<table class="table table-striped table-bordered table-responsive" style="margin-top:20px">
    <tr>
        <th>BookingId</th>
        <th>Customer Name</th>
        <th>CheckIn</th>
        <th>CheckOut</th>
        <th>No.of Person</th>
        <th>Assigned Room</th>
        <th>Total Amount</th>
    </tr>
    @foreach (var data in Model)
    {
        <tr>
            <td>@data.BookingId</td>
            <td>@data.CustomerName</td>
            <td>@data.BookingFrom.ToShortDateString()</td>
            <td>@data.BookingTo.ToShortDateString()</td>
            <td>@data.NoOfMembers</td>
            <td>@data.RoomNumber</td>
            <td>@data.TotalAmount</td>

        </tr>
    }
</table>